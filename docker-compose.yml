services:
  postgres:
    image: postgres:17
    container_name: eventos-db
    environment:
      POSTGRES_DB: eventos
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - eventos_data:/var/lib/postgresql/data

volumes:
  eventos_data: